/**
 *
 * @param {number[]} ints
 * @param {number} s
 * @returns
 */
function sumPairs(ints, s) {
  const pairs = [0, 0];

  ints.forEach((num) => {
    if (pairs[0] + pairs[1] === 0) {
      return;
    } else return;
  });

  return [0, 0] || undefined;
}
