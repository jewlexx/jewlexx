# Visit https://github.com/lowlighter/metrics#-documentation for full reference
name: Metrics
on:
  # Schedule updates (each hour)
  schedule: [{ cron: '0 * * * *' }]
  # Lines below let you run workflow manually and on each commit
  workflow_dispatch:
  push: { branches: ['master', 'main'] }
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          # Your GitHub token
          # The following scopes are required:
          #  - public_access (default scope)
          # The following additional scopes may be required:
          #  - read:org      (for organization related metrics)
          #  - read:user     (for user related data)
          #  - read:packages (for some packages related data)
          #  - repo          (optional, if you want to include private repositories)
          token: ${{ secrets.METRICS_TOKEN }}

          # Options
          filename: github-metrics.svg
          committer_branch: metrics
          user: jewlexx
          config_timezone: Australia/Sydney
          config_order: base.header, introduction, base.activity+community, base.repositories, code, languages
          template: classic
          base: header, repositories, activity, community
          # plugin_code: yes
          # plugin_code_days: 14
          # plugin_code_languages: rust, c, javascript, typescript, dart
          # plugin_code_lines: 12
          # plugin_code_load: 400
          # plugin_code_visibility: public
          # plugin_habits: yes
          # plugin_habits_days: 14
          # plugin_habits_facts: yes
          # plugin_habits_from: 200
          # plugin_lines: yes
          # plugin_lines_history_limit: 1
          # plugin_lines_repositories_limit: 4
          # plugin_lines_sections: base
          plugin_languages: yes
          plugin_languages_analysis_timeout: 600
          plugin_languages_analysis_timeout_repositories: 60
          plugin_languages_limit: 8
          plugin_languages_skipped: Notes, FolderSize
          plugin_languages_threshold: 0%
